<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!--
        windowLightStatusBar diz "icones escuros", para fundo claro. Ele existe aqui, e nao
        so na chamada de StatusBar.setStyle em JS, porque a saida da splash do Android 12+
        devolve a aparencia da barra ao valor do tema da janela: sem esta linha o setStyle
        era aplicado e desfeito logo em seguida, deixando icones brancos sobre o tema claro
        em toda abertura a frio. O values-night/ ao lado inverte para o tema escuro.
    -->
    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>


    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="android:background">@drawable/splash</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>
</resources>